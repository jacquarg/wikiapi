{
  "name": "wikiapi",
  "version": "0.0.1",
  "description": "Un wiki pour les API publiques (et pas ^^ ?)",
  "scripts": {
    "deduplicateids": "node scripts/check_ids.js",
    "build": "npm run deduplicateids && node scripts/build_mesinfosdev.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "glob": "^7.1.2",
    "jsonld": "0.4.12"
  }
}
